{% set version = "2.7.4" %}
{% set sha256 = "8bf51191d60f6987482ef0cfe8511bbf4877a5aa7f313d7b488b53189cf26209" %}

package:
  name: psycopg2
  version: {{ version }}

source:
  fn: psycopg2-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/p/psycopg2/psycopg2-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - have_openssl.patch

build:
  number: 0
  script: python setup.py install  # [win]

requirements:
  build:
    - python
    - openssl 1.0.*
    - postgresql
  run:
    - python
    # libpq is a repackaging of only the library to connect to postgres
    - libpq
    - openssl 1.0.*

test:
  imports:
    - psycopg2
    - psycopg2._psycopg

about:
  home: http://initd.org/psycopg/
  license: LGPL, BSD-like, ZPL
  summary: 'Python-PostgreSQL Database Adapter'

extra:
  recipe-maintainers:
    - msarahan
    - ocefpaf
